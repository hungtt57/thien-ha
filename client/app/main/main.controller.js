'use strict';

angular.module('thienHaApp')
  .controller('MainCtrl', function ($scope, $http,cmsConfig,$location) {
  	 // var url = cmsConfig['api-url'] + '/api/sites/' + cmsConfig['siteId'];
  	 // $http.get(url).success(function(data){
  	 // 	var state = data.released_version.toLowerCase();
    //   if(state !== 'official') {
    //     $location.path('/prel');
    //   }
  	 // });
  });
